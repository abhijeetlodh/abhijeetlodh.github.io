@workspace I need you to act as a Senior Data Engineer working on a Dataiku DSS Agent Tool for clinical trial forecasting. We are migrating to a new "Flex with Cycles" protocol, but our existing database lacks the mandatory parameters. We must extract this missing data directly from PPW (Protocol Parameters Worksheet) PDFs.

Please analyze the three open files:
1. `Protocol_data_retriever.py`: This is the legacy database retrieval script. 
2. `Impala2PPW.py`: This will be our new PDF extraction tool. 
3. `Flex Cycles PPw impala 2 datamapping.xlsx`: This defines the business logic and target variables (pay special attention to any row marked "PPW-driven").

**Your Mission:**
Refactor `Impala2PPW.py` to replace the old SQL/database extraction logic with a new PDF-to-LLM pipeline, while preserving the exact output payload structure found in `Protocol_data_retriever.py`.

**Step-by-Step Implementation Requirements:**
1. **Preserve the Shell:** Keep the `BaseAgentTool` class inheritance, the `get_descriptor()` input schema (which takes `study_code`), and the main `invoke(self, input, trace)` signature.
2. **PDF Ingestion Logic:** Write a new private method (e.g., `_get_pdf_stream`) that takes the `study_code` and simulates fetching the matching PDF from a Dataiku managed folder.
3. **Text Extraction:** Write a method using `pdfplumber` to extract raw text from the first 30 pages of the PDF, filtering for tables containing keywords like "VISIT", "CYCLE", "DISPENSING", and "TREATMENT".
4. **LLM Sanitization (Crucial):** Write a method `_sanitize_with_llm(raw_text, study_code)`. Use Dataiku's LLM Mesh (`from dataiku.llm import Model`). The system prompt must instruct the LLM to clean the messy raw text, fix malformed visit IDs, and output strictly formatted JSON. 
5. **Data Mapping:** The LLM's target JSON schema must perfectly match the payload dictionary built at the end of `Protocol_data_retriever.py` (specifically the `visits`, `kits`, `dispensing_sets`, and `dispense_rules` arrays). Use the `.xlsx` mapping file to ensure the LLM knows what fields to populate.
6. **Error Handling:** Ensure the `invoke` method cleanly catches exceptions and returns the standard `{"output": {"success": False, "error_message": "..."}}` format.

**Output Generation:**
Do not explain standard Python concepts. Output the complete, refactored code for `Impala2PPW.py`. After generating the code, provide a standalone `test_impala2ppw.py` script using `unittest` and a mocked PDF/LLM response so I can immediately validate the payload structure locally before pushing to Dataiku.
